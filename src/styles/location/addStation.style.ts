import styled from "styled-components/native";
import { Button, Card, Dialog, Portal, IconButton } from "react-native-paper";

export const AddStationLayout = styled.View`
  flex: 1;
  padding: 20px;
`;

export const CurrentLocationCard = styled(Card)``;

export const CardRow = styled.View`
  flex-direction: row;
`;

export const Title = styled.Text``;

export const Text = styled.Text``;

export const StationNotFound = styled.View``;

export const AddStation = styled(Card)``;

export const AddButton = styled(IconButton)``;
